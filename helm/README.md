# Helm

This folder contains the Helm chart for Lab 03.

Chart:

```text
helm/lab-microservices
```

Install example:

```powershell
helm upgrade --install lab03 .\helm\lab-microservices --namespace lab03 --create-namespace
```

