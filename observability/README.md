# Observability

This folder contains Lab 09 observability assets.

- `loki-stack-values.yaml` configures a local Loki + Promtail + Grafana + Prometheus stack.
- `log-generator.yaml` creates a small secured pod that continuously writes sample logs.

The stack is intended for a local Docker Desktop Kubernetes lab environment.
